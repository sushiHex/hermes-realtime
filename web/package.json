{
  "name": "hermes-realtime-client",
  "private": true,
  "version": "0.0.3",
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit && node build.mjs",
    "test": "vitest run"
  },
  "dependencies": {
    "livekit-client": "^2.15.6"
  },
  "devDependencies": {
    "@types/jsdom": "^21.1.7",
    "@types/node": "^22.20.1",
    "esbuild": "^0.25.6",
    "jsdom": "^26.1.0",
    "typescript": "^5.8.3",
    "vitest": "^5.0.0"
  }
}
